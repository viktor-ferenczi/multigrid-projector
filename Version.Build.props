<Project>
  <PropertyGroup>
    <!-- Plugin version, change it here at a single place -->
    <Version>0.9.4</Version>
    <AssemblyVersion>$(Version)</AssemblyVersion>
    <FileVersion>$(Version)</FileVersion>
  </PropertyGroup>
</Project>
